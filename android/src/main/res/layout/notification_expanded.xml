<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_height="64dp"
    android:padding="6dp">

    <ImageView
        android:id="@+id/collapsedNotificationImage"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:background="#FFFFFF"
        android:cropToPadding="true"
        android:scaleType="centerCrop"
        android:src="@mipmap/ic_launcher"
        tools:src="@mipmap/ic_launcher" />

    <TextView
        android:id="@+id/collapsedNotificationTitle"
        style="@style/TextAppearance.Compat.Notification.Title"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_toRightOf="@id/collapsedNotificationImage"
        android:ellipsize="end"
        android:gravity="left"
        android:maxLines="2"
        android:padding="5dp"
        android:text="Checkout these new meme we found for you on Chomu , Click to see more." />




</RelativeLayout>